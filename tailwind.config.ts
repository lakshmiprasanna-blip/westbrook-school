import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#0F4D81",
        maroon: "#9B1B2F",
        dark: "#2B292A",
        lightblue: "#A2D5EB",
      },
    },
  },
};

export default config;
